---
title: "Docker Setup"
tier: guide
---

# Docker Setup Guide

This guide covers Docker setup for development.

## Prerequisites

- Docker installed
- Docker Compose installed

## Getting Started

Build the containers:

```bash
docker-compose build
```

Start the application:

```bash
docker-compose up -d
```

## Configuration

Configure environment variables in `.env` file.
